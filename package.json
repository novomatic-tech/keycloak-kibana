{
  "name": "keycloak-kibana",
  "version": "3.0.0-alpha7",
  "description": "A Keycloak authorization plugin for Kibana",
  "kibana": {
    "location": "https://github.com/elastic/kibana/archive/v7.2.0.tar.gz"
  },
  "scripts": {
    "install": "node scripts/install.js",
    "build": "node scripts/build.js",
    "start": "node scripts/start.js --dev --oss --no-base-path",
    "kbn": "node kibana/scripts/kbn",
    "lint": "node scripts/lint.js",
    "test": "node scripts/test.js"
  },
  "devDependencies": {
    "tar": "^4.4.8"
  },
  "engines": {
    "node": "10.15.2",
    "yarn": "^1.10.1"
  }
}
